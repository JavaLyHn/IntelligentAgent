<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.intelligentagent.mapper.ExecutionRecordMapper">

    <select id="selectPageByWorkflowId" resultType="com.intelligentagent.entity.ExecutionRecord">
        SELECT * FROM execution_records
        WHERE workflow_id = #{workflowId}
        ORDER BY created_at DESC
    </select>

    <select id="countByWorkflowId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM execution_records
        WHERE workflow_id = #{workflowId}
    </select>

</mapper>
